import type { IBaseHeader } from '../../types/data'
import { ref } from 'vue'

export const helpers = ref<IBaseHeader>({
    label:'Canvas Helpers',
    desctiption:'Simple handwriting pad.',
    title: 'Canvas',
    sub:'Helpers',
    url: '/layout/canvasDemo'
})